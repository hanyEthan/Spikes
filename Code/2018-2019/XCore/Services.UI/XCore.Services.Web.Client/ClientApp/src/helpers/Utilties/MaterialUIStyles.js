import { makeStyles } from '@material-ui/core/styles';
export const useStyles_Form = makeStyles(theme => ({
    container: {
     
      marginLeft:'10%'
    },
    textField: {
      marginLeft: theme.spacing(1),
      marginRight: theme.spacing(1),
      width: 200,
    },
  }));